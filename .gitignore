node_modules/
.env
.env.*

# generated oracle snapshot
oracle/root.json

# circuit build artifacts and ceremony output
circuits/build/
circuits/.deps/
*.zkey
*.r1cs
*.wasm
*.ptau

# per-user proof and challenge files (may carry session nonces)
proof.json
challenge.json
*.proof.json

# a member's two-tier secret, never commit
*.secret.json
member.secret*

# durable registration records written at runtime by the gateway
data/

# transient witness inputs written by circuit make_input helpers
input.json
expected.txt
witness.json

# editor and OS noise
.DS_Store
*.log
