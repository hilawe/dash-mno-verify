[package]
name = "methods"
version = "0.1.0"
edition = "2021"

[build-dependencies]
# Align with your installed RISC Zero release (see the top-level README).
risc0-build = { version = "1.2.0" }

[package.metadata.risc0]
methods = ["guest"]
